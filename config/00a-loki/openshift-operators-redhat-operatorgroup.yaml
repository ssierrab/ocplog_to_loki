# For cluster where Loki runs. Used by Loki Operator subscription.
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: loki-operator
  namespace: openshift-operators-redhat
spec: {}
