# Same operator as openshift-logging-operator-subscription.yaml; alternate file for docs that reference "v6" manifest.
# __OPERATOR_CHANNEL__ — same as Loki; use render script or make (see README).
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: cluster-logging
  namespace: openshift-logging
spec:
  channel: __OPERATOR_CHANNEL__
  installPlanApproval: Manual
  name: cluster-logging
  source: redhat-operators
  sourceNamespace: openshift-marketplace
