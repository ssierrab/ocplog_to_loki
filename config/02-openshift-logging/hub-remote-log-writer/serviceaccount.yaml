# HUB only: ServiceAccount used to mint a long-lived token for spokes to authenticate
# to the LokiStack gateway (Bearer token in Secret to-loki-secret on the spoke).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: remote-log-writer
  namespace: openshift-logging
