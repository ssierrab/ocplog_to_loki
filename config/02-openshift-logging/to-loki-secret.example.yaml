# SPOKE only: Secret consumed by ClusterLogForwarder for external (hub) Loki.
# Create with oc create secret (see README §2.4.2) after obtaining token + CA from the hub.
#
# Keys:
#   token         — raw JWT only (do NOT prefix with "Bearer "; the collector adds it).
#   ca-bundle.crt — PEM that trusts the hub Loki HTTPS endpoint (Route *.apps → router-ca; see README §2.4.2).
apiVersion: v1
kind: Secret
metadata:
  name: to-loki-secret
  namespace: openshift-logging
type: Opaque
stringData:
  token: "<paste-raw-jwt-from-hub-oc-create-token>"
  ca-bundle.crt: |
    -----BEGIN CERTIFICATE-----
    ...
    -----END CERTIFICATE-----
